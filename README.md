# data-digger-sql-project
This project demonstrates a basic e-commerce database system using MySQL. It includes the creation of relational tables such as Customers, Products, Orders, and OrderDetails with proper constraints. The project also covers data insertion and execution of various SQL queries including filtering, sorting, joins, and aggregate functions.
